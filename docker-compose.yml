services:
  web360ktx:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3004:3004"
    volumes:
      - .:/app
      - /app/node_modules
    stdin_open: true
    tty: true
